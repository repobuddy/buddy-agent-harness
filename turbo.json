{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
		"test": { "dependsOn": ["^build", "build"] },
		"typecheck": { "dependsOn": ["^build"] },
		"verify": { "dependsOn": ["build", "typecheck", "test"] }
	}
}
