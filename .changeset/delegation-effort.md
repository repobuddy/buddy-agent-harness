---
'buddy-agent-harness': patch
---

The `enhance` skill's `## Delegation` section now also tells the agent how to pick a subagent's reasoning effort: the lowest, unless it could not write down the steps that reach the answer.

A repository holding the previous wording is offered the new one as a replacement.
