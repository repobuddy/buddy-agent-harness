{"kind":"leash","cr-ref":"110","leash":"auto-spec","by":"derived","approach":["Scaffold the skills/enhance/ node from the CR 35 design draft, resolving its five blocking findings, and carry the new stale-detection path in the same node. The corpus gap and the CR's behavior are one subject; splitting them would spec the skill twice.","Blast radius: one shipped skill body, one shipped reference file, the spec corpus, and the docs page. Published package changes, so a changeset is owed.","The frozen Delegation wording is not touched. This CR changes when the canonical text is surfaced, never what it says.","Stale is a recognizable earlier form of the addition, not semantic proximity. The user's own prose is covered, never stale."]}
{"kind":"gate","cr-ref":"110","gate":"spec","verdict":"approve","by":"agent","node":"skills/enhance","why":"Cold spec-judge returns ALIGNED with oracle, builder and architect all passing on round two. Round one blocked at governance pre-flight and graded no lens: the conductor authored inline without the lifecycle and gate-validation governances and without the plugin-tier builder bar the project registry names for artifact-type skill, the same miss that failed CR 35's first round. Composing that bar surfaced two shipped rules with no scenario, both now covered; the second is a barred row rather than a use-case extension because no decision in the drawn graph reaches a file outside the agent configuration, and the judge traced the graph and confirmed it. The node also closes the corpus gap CR 35 left open, resolving all five of its blocking findings. Suite and spec-state checks clean, 26 scenarios, map 1:1 by manual trace. Self-asserted within the derived auto-spec leash."}
{"kind":"followup","cr-ref":"110","class":"backlog","what":"Staleness is anchored on spans matched character for character, so a repository that took the earlier wording and then hand-edited a clause carries no surviving span and is never offered the current text. The impl gate built this case and confirmed it. It is a deliberate trade rather than a defect: the node states that a missed offer costs nothing while overwriting the owner's own words is the expensive failure, and no frozen scenario requires catching a locally edited copy. Worth revisiting if consumers turn out to edit an addition in place more often than expected."}
{"kind":"gate","cr-ref":"110","gate":"impl","verdict":"pause","cause":"recurrence-replan","cause-candidate":true,"node":"skills/enhance","why":"Twenty-five of twenty-six frozen scenarios pass across five rounds. The safety scenario, which requires the skill to leave alone a section it cannot tell from the owner's own words, failed in rounds two through five. Each round the judge built an owner-voiced section that satisfied the criterion standing at the time, and each fix was verified against every earlier counter-example, so no hole was traded for another. Round five classifies its finding a second consecutive recurrence of the provenance class and argues the mechanism rather than the chosen span is unsound: a distinctive phrase is reproducible by anyone who has read this package's documentation. That premise was checked against a hand-maintained instruction file outside this repository and found to be false as the criterion assumes it. The remaining choice is between accepting a mis-offer risk, writing a provenance marker on approval which helps no repository that already carries the earlier wording, and offering the difference without asserting whose text it is, which would narrow a frozen scenario and fires Clearance. That is the owner's call, so the gate pauses."}
{"kind":"followup","cr-ref":"110","class":"blocking","what":"Contradicts this mission's own completion claim: the stale-detection path is specified and its skill body shipped, but the criterion that decides staleness does not yet meet the frozen safety scenario. Deciding how a shipped addition proves a section descends from it is the open question. Three candidates were reached: accept a verbatim-span match with its mis-offer risk; have the skill write a provenance marker when it writes an addition, which is sound but does nothing for the repositories already carrying the earlier wording, who are the population the issue names; or drop the provenance claim entirely and offer the difference for the owner to judge, which needs a frozen scenario narrowed and so needs clearance. Filed on the forge."}
