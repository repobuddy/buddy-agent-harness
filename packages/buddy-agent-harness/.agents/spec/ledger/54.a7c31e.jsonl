{"kind":"leash","cr-ref":"54","leash":"auto-all","by":"derived","approach":["land the read-only detection half only; writers follow as a second CR","settle the canonical-location open question against primary sources before fixing the golden filename","new module under src/diagnose-mcp/, reported through doctor's existing findings and help sections","secret values are redacted at the source: no value ever enters a finding object"],"blast":"new CLI detection surface, one new runtime dependency, new published findings vocabulary"}
{"kind":"followup","cr-ref":"54","class":"backlog","what":"cli/configuration-diagnosis justifies ignoring --harness with 'every check here requires a projection to exist on disk', which is not true of its unread-local-override and unloadable-skill checks — neither involves a projection or a harness. The conclusion holds; the stated mechanism does not cover two of the four faults.","where":"the sibling node's Surface section","note":"a structural point in another capability, deferred rather than fixed here; it sits inside the doctor spec backfill already open as issue 58 rather than warranting its own"}
{"kind":"followup","cr-ref":"54","class":"backlog","what":"forward projection of the golden MCP set into each harness target, and the approval-gated per-server per-field reconcile back into it, including the refusal to auto-merge a three-way conflict and the refusal to pull a literal credential into the golden set. The reverse converters and both baselines landed with this CR; the writers need a write-capable home.","where":"a second CR against issue 54's remaining scope"}
{"kind":"followup","cr-ref":"54","class":"backlog","what":"a community report that Copilot CLI gained a project-scope MCP file could not be confirmed against a reachable primary source and was recorded unverified (E-MCP-11). Re-check GitHub's own documentation before Copilot CLI is left out of the MCP registry again.","where":".research/mcp-canonical-location/evidence.md"}
{"kind":"followup","cr-ref":"54","class":"backlog","what":"MCP standards-track proposals SEP-2633 and discussion 2218 are open and unratified. If either is accepted with a stated directory convention it would be the first primary-sourced answer to where MCP configuration lives, and the trigger to revisit the golden set's path.","where":".research/mcp-canonical-location/conclusion.md"}
{"kind":"gate","cr-ref":"54","gate":"spec","verdict":"approve","by":"agent","cause":"dimension","why":{"floor":"none","blast":"low — a new read-only finding family on an existing command; nothing existing changes shape","novelty":"high — a new canonical file the project defines, and a credential scanner whose output lands in every session transcript","confidence":"high — three lenses passed cold on the remediated spec, the no-leak invariant is pinned per code path, and each pin was miss-tested by injecting a leak"},"frozen":["cli/mcp-diagnosis/mcp-diagnosis.feature"],"note":"the root spec stays draft: the corpus is mid-backfill and cli/ is still a stub, so approving one node is recorded here and by the freeze rather than by claiming the project spec is approved"}
