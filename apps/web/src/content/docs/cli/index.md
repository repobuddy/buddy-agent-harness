---
title: CLI Reference
description: Command-line interface for initializing harness skill links.
---

`buddy-agent-harness` provides the `init` command for creating a canonical skill directory and synchronizing it with selected harness locations.

```sh
npx -y buddy-agent-harness init
```

See [`init`](/cli/init/) for flags, output, and conflict behavior.
